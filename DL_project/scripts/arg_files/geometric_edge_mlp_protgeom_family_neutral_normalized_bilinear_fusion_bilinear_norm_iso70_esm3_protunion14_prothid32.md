# geometric_edge_mlp_protgeom_family_neutral_normalized_bilinear_fusion_bilinear_norm_lcs_esm3_protunion14_prothid32, measured at a REQUESTED distance from training.
#
# Differs from geometric_edge_mlp_protgeom_family_neutral_normalized_bilinear_fusion_bilinear_norm_lcs_esm3_protunion14_prothid32.md
# in ONE line: --lipid_coldsplit is replaced by --lipid_isolation=0.70. Every other
# flag -- the 14-descriptor protein union, --protein_hiddim=32, ESM3 embeddings, the
# sampler, the epochs -- is copied over untouched, so the five siblings and the four
# named-set runs form one curve of one configuration.
#
# The block. --lipid_isolation takes the key of a block in
# dataloader/lipid_isolation_blocks.py, generated by analysis/lipid_block_search.py:
# 47 lipid species, 103 positives on the full table, chosen so that the block's
# isolation -- the mean over its structures of the best Tanimoto similarity to a
# structure still in training -- lands on 0.70. Its chemistry: фосфатидаты 23%, церамиды 19%, триацилглицеролы 15%, ещё 16 классов.
# Reconstructed at seed 0 with this file's sampler, the TEST half holds 51
# positives over 18 proteins at isolation 0.701, and no evaluated lipid is in
# training (seen share 0.000).
#
# Why this rung: closes the 0.653-0.767 gap, the only one left in the middle of the axis.
#
# Same split in every other respect as --lipid_coldsplit: every protein stays in
# training, the held-out rows are halved label-by-label into validation and test, and
# the pooled BA/F1 caveat of files/lipid_coldsplit_architecture_direction.md's RULE box
# applies here too.
#
# Not yet run.

--ep=120
--fast_attention
--hiddim=8
--dropout=0.1
--weight_decay=0.01
--pool_type="add"
--bilinear_fusion
--bilinear_pooled_norm
--protein_descriptors=pocket_volume_per_sasa,pocket_elongation,pocket_flatness,buriedness_q50,apolar_sasa_share,aromatic_share,hydropathy_rim,ev28_q10,aromatic_share_rim,depth_q10,hydropathy_core,ev14_q10,hydropathy_mean,pocket_extent
--protein_edge_mlp
--protein_hiddim=32
--save_model_in_dynamics
--balanced_batches
--balanced_proteins
--lipid_isolation=0.70
